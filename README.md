# Portfolio

A simple static portfolio scaffold built with Bootswatch (Cosmo) and Bootstrap 5.

## Running locally

From the repo root:

```bash
# Option 1: Python HTTP server
python -m http.server 8000

# Option 2: live-server (if installed via npm)
live-server
